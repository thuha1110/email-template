// Background service worker (MV3). Reserved for future cloud sync/API integrations.
chrome.runtime.onInstalled.addListener(() => {
  // Initialize default categories or perform migrations if needed.
});
